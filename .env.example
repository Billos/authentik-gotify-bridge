# Server configuration
PORT=3000

# Gotify configuration
# URL of your Gotify server (without trailing slash)
# Required: At least one endpoint token must be configured for the service to be useful
GOTIFY_URL=https://push.example.de

# Optional: Gotify token for Authentik webhook endpoint (/webhook)
# Leave empty or comment out to disable this endpoint
GOTIFY_TOKEN_AUTHENTIK=your-gotify-app-token-here

# Optional: Gotify token for Slack webhook endpoint (/slack)
# Leave empty or comment out to disable this endpoint
GOTIFY_TOKEN_SLACK=your-gotify-app-token-here
